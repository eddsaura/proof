# Proof Social

This repo is now organized under `apps/`.

## Structure

- `apps/mobile` is the active React Native + Expo + Convex app
- Web deployment is handled from `apps/mobile` with Expo web export
